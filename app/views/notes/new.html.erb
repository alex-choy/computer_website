<%= form_for @note, url: {action: "create"} do |f| %>

	<%= f.label :title%><br>
	<%= f.text_field :title %> <br><br>

	<%= f.label :content %><br>
	<%= f.text_area :content %><br>
	<%= f.submit "Create" %>

<% end %>